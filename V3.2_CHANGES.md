# v3.2 代码改造完成清单

## 改动文件清单

### 1. main.py（主程序）
**改动内容：**
- 移除旧的 train/test 配置方式
- 改为 WFO + OOS 测试模式
- 新增命令行参数：
  - `--oos-mode`: 选择 OOS 测试模式（primary: 2025-2026 或 stress_test: 2021-2023）
  - `--pool-size`: 配置股票池大小（50-80，默认80）
- 全样本周期用于 WFO 训练（20180101-20260310）
- OOS 测试集独立配置

### 2. stock_pool.py（股票池模块）
**改动内容：**
- `build_stock_pool()` 函数新增 `target_size` 参数
- 支持 50-120 只股票池可配置（默认80）
- 更新日志输出，显示目标数量和实际数量

### 3. config.py（配置文件）
**已符合 v3.2 要求，无需改动：**
- ✅ 全样本周期：20180101-20260310
- ✅ WFO 配置：训练3年 + 验证6个月 + 滚动6个月
- ✅ OOS 测试集：primary(2025-2026) 和 stress_test(2021-2023)
- ✅ 股票池目标：80-120只（可扩展到50-80）
- ✅ v3.2 信号逻辑：加权评分系统（score_threshold: 0.65）
- ✅ 优化器惩罚逻辑：交易次数>=50笔、换手率控制

### 4. signal_generator.py（信号生成模块）
**已符合 v3.2 要求，无需改动：**
- ✅ 加权评分系统（7个维度）
- ✅ 评分阈值：0.65（可配置0.60-0.70）
- ✅ 流动性过滤：3000万成交额

### 5. optimizer.py（优化器模块）
**已符合 v3.2 要求，无需改动：**
- ✅ 遗传算法：种群100、代数100
- ✅ 交易次数惩罚：<50笔显著惩罚
- ✅ 换手率惩罚：年化>200%惩罚
- ✅ 参数空间：theta_buy(-8~-5)、score_threshold(0.60~0.70)

## v3.2 配置确认

### 数据集配置
```python
DATASET_CONFIG = {
    'full_period': {
        'start_date': '20180101',
        'end_date': '20260310'
    },
    'wfo': {
        'train_window_years': 3,      # 训练窗口：3年
        'val_window_months': 6,       # 验证窗口：6个月
        'step_months': 6              # 滚动步长：6个月
    },
    'oos_test': {
        'primary': {
            'start_date': '20250101',
            'end_date': '20260310',
            'description': '主要样本外测试集 (2025-2026)'
        },
        'stress_test': {
            'start_date': '20210101',
            'end_date': '20231231',
            'description': '压力测试集 (2021-2023)'
        }
    }
}
```

### 股票池配置
```python
POOL_CONFIG = {
    'target_size': (80, 120),  # 目标80-120只（可扩展到50-80）
    'min_market_cap': 500000,
    'min_avg_amount': 50000,
    'min_list_days': 365
}
```

### 策略参数（v3.2）
```python
STRATEGY_PARAMS = {
    'theta_buy': -6.0,         # BIAS评分参考值
    'theta_sell': 15.0,
    'alpha_vol': 0.65,         # 缩量系数（0.5~0.8）
    'rsi_thresh': 35,          # RSI评分参考值
    'score_threshold': 0.65,   # 信号评分阈值（0.60-0.70）
    'risk_per_trade': 0.02,
    'max_position': 0.8,
    'min_amount': 30000000     # 3000万成交额
}
```

## 副机运行命令

### 1. 基础运行（使用默认参数，跳过优化）
```bash
cd /root/.openclaw/agents/shared/workspace/quantv3
python main.py --skip-optimize
```

### 2. OOS 主测试集（2025-2026）
```bash
python main.py --skip-optimize --oos-mode primary --pool-size 80
```

### 3. OOS 压力测试集（2021-2023）
```bash
python main.py --skip-optimize --oos-mode stress_test --pool-size 80
```

### 4. 完整 WFO 优化（耗时较长，不推荐立即运行）
```bash
# 警告：此命令会执行完整的遗传算法优化，耗时可能数小时
python main.py --oos-mode primary --pool-size 80
```

### 5. 扩展股票池到50只（快速测试）
```bash
python main.py --skip-optimize --oos-mode primary --pool-size 50
```

### 6. 扩展股票池到120只（完整测试）
```bash
python main.py --skip-optimize --oos-mode primary --pool-size 120
```

## 命令行参数说明

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `--version` | str | v3.2 | 版本号 |
| `--skip-optimize` | flag | False | 跳过参数优化，使用默认参数 |
| `--oos-mode` | str | primary | OOS测试模式：primary(2025-2026) 或 stress_test(2021-2023) |
| `--pool-size` | int | 80 | 股票池大小（50-120） |

## 输出目录

所有结果输出到 `./result_v3.2/` 目录：
- `backtest_report_v3.2.md` - 回测报告
- `equity_curve_v3.2.png` - 净值曲线图

## 注意事项

1. **禁止运行的命令**（会触发长耗时优化）：
   ```bash
   # ❌ 不要运行（会执行完整优化）
   python main.py
   python main.py --oos-mode primary
   ```

2. **推荐运行的命令**（快速测试）：
   ```bash
   # ✅ 推荐（跳过优化）
   python main.py --skip-optimize
   python main.py --skip-optimize --oos-mode stress_test
   ```

3. **数据库连接**：
   - 确保副机可以访问数据库：100.87.204.122:5432
   - 数据库配置在 `config.py` 中

4. **依赖检查**：
   ```bash
   pip install pandas numpy psycopg2-binary deap matplotlib
   ```

## v3.2 改进总结

1. ✅ 数据集配置：全样本周期 20180101-20260310
2. ✅ WFO 配置：训练3年 + 验证6个月 + 滚动6个月
3. ✅ OOS 测试集：primary(2025-2026) 和 stress_test(2021-2023)
4. ✅ 股票池：支持 50-120 只可配置
5. ✅ 信号逻辑：加权评分系统（7维度，阈值0.65）
6. ✅ 优化器：交易次数>=50笔、换手率控制
7. ✅ 代码层面准备完成，未执行任何回测或调参

## 快速验证

在副机上运行以下命令验证配置：
```bash
cd /root/.openclaw/agents/shared/workspace/quantv3
python -c "from config import DATASET_CONFIG; import json; print(json.dumps(DATASET_CONFIG, indent=2))"
```

预期输出应显示正确的 v3.2 配置。
